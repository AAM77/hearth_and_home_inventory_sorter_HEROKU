<%= erb(:"partials/flash_partial") %>

<h3> [ -- <%= @category.name %> -- ] contains the Following Items: </h3>
<br>
<br>

<!-- Lists the items in the category (alphabetical Order) -->
<% @category.items.sort { |a,b| a.name.downcase <=> b.name.downcase }.each do |item| %>

  <!-- Lists the items in the category (alphabetical Order) -->
  <!-- Displays an item url + edit and delete buttons -->

  <!-- LIST ITEM PARTIAL -->
  <%= erb(:"partials/list_item_partial", :locals => {:collapse_num => "collapseOne", :object_one => category, :object_two => item, :object_one_plural => "categories", :object_one_slug => category.slug, :object_one_id => category.id, :object_two_plural => "items", :object_two_slug => item.slug, :object_two_id => item.id })%>
  <!-- LIST ITEM PARTIAL -->

<% end %> <!-- category.items.each -->
<br>
<br>

<form action="/<%= current_user.slug %>/items/new">
<button class="content_button" type="submit"><a class="add_button" href="/<%= current_user.slug %>/items/new">Add a New Item</a></button>
</form>

<br>
<br>

<form action="/<%= current_user.slug %>/categories/<%= @category.slug %>/edit">
  <button class="content_button"  type="submit">Edit Category</button>
</form>

<form action="/<%= current_user.slug %>/categories/<%= @category.slug %>/delete" method="POST">
  <input id="hidden" type="hidden" name="_method" value="delete">
  <button class="content_button"  type="submit">Delete Category</button>
</form>


  <br>
  <br>

  <a href="javascript:history.back()">Go Back</a>
