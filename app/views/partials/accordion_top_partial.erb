<div class="accordion" id="accordionExample">
  <div class="card">
    <div class="card-header" id="<%= heading_num %>">
      <h5 class="mb-0">
        <button class="btn btn-light btn-lg btn-block" type="button" data-toggle="collapse" data-target="#<%= collapse_num %>" aria-expanded="false" aria-controls="<%= collapse_num %>">
